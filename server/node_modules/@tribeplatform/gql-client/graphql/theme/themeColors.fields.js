import { themeColorGQLFields } from './themeColor.fields.js';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const themeColorsGQLFields = (fields) => `
  __typename
  dark {
    ${themeColorGQLFields('all')}
  }
  light {
    ${themeColorGQLFields('all')}
  }
`;
