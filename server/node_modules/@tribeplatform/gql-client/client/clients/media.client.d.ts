import { ClientFileErrorHandler } from '../../lib/index.js';
import { CreateEmojiInput, CreateImageInput, SignedUrl, Image, Emoji, UploadImagesArgs, Embed, EmbedInput, MutationUpdateImageArgs, CreateFileInput, AttachmentUploadType, File } from '../../types/index.js';
import { GraphqlClient } from '../graphql.client.js';
export declare class MediaClient {
    private client;
    constructor(client: GraphqlClient);
    embed(variables: EmbedInput): Promise<Embed>;
    updateImage(variables: MutationUpdateImageArgs): Promise<Image>;
    createEmojis(input: CreateEmojiInput[]): Promise<Array<Emoji>>;
    createImages(variables: CreateImageInput[]): Promise<Array<SignedUrl>>;
    createFiles(variables: CreateFileInput[]): Promise<Array<SignedUrl>>;
    uploadFiles(input: AttachmentUploadType, handleError?: ClientFileErrorHandler): Promise<File[]>;
    uploadImages(input: UploadImagesArgs[], handleError?: ClientFileErrorHandler): Promise<Array<Image>>;
}
