export declare type Paginated<T> = 'basic' | 'all' | CustomPaginated<T>;
export interface CustomPaginated<T> {
    edges?: {
        cursor?: 'all';
        node?: T;
    };
    nodes?: T;
    pageInfo?: {
        endCursor?: 'all';
        hasNextPage?: 'all';
    };
    totalCount?: 'all';
}
export declare function paginatedGQLFields<T>(fields: Paginated<T>, innerGQLFieldsFn: (fields: T) => string): string;
