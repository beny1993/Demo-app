import { mediaURLGQLFields } from './mediaURL.fields.js';
export const signedUrlGQLFields = () => {
    return `
    fields
    mediaId
    mediaUrl
    signedUrl
    urls {
      ${mediaURLGQLFields()}
    }
  `;
};
