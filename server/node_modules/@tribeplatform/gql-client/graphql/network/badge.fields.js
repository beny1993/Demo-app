import { mediaGQLFields } from '@tribeplatform/gql-client';
export const badgeGQLFields = (fields) => `
  active
  backgroundColor
  daysUntilExpired
  id
  image {
    ${mediaGQLFields('basic')}
  }
  imageId
  longDescription
  name
  shortDescription
  textColor
  type
`;
