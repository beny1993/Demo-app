import { blockGQLFields } from './block.js';
export const slateGQLFields = (fields) => `
  id
  rootBlock
  blocks {
    ${blockGQLFields(fields)}
  }
  restrictions {
    nonEditableBlocks
    lockedChildrenBlocks
    nonRemovableBlocks
  }
`;
